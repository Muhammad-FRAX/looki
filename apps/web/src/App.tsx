import { Routes, Route } from 'react-router-dom';

export default function App() {
  return (
    <Routes>
      <Route path="/" element={<div style={{ padding: 32 }}>Looki — coming soon</div>} />
    </Routes>
  );
}
